name: CI

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  # PRs validate packaging/scripts against the published image — full compiles
  # happen only in the Release workflow when compile inputs change.
  IMAGE: ghcr.io/${{ github.repository }}:latest

jobs:
  # Required check name stays stable for branch protection.
  build-and-test:
    name: Build & smoke test
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Pull published image
        run: |
          set -euo pipefail
          echo "Using published image: ${{ env.IMAGE }}"
          docker pull "${{ env.IMAGE }}"

      - name: Smoke test
        run: |
          chmod +x scripts/smoke-test.sh
          ./scripts/smoke-test.sh "${{ env.IMAGE }}"
